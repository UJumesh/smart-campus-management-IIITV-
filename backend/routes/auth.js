const express = require("express");
const router = express.Router();

// This file is just a placeholder for future auth routes
// The main auth routes are currently defined in server.js

module.exports = router;
